#include <stdio.h>
#include "algo.h"
#include "nodes.h"
#include "edges.h"

int main() {
	while (true) {
		char cmd;
		scanf("%c", &cmd);

		switch (cmd) {
			case 'A':
				if (listIsNull()) {
				/	deleteList();
			//	
				//createUserList();
				break;
			case 'B':
				break;
			case 'S':
				break;
			case 'T':
				break;
			default:
				break;
		}
	}
	return 0;
}
